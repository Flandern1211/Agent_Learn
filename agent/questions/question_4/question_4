懒加载

- 网页开发中，即对某些资源不进行全部加载，而是在用户要浏览到对应资源的时候才给予加载
- Go 的 sync.Once：第一次调用时才初始化，后续复用
- 数据库 ORM 的关联查询：你访问 user.Order 时才去查订单表，不是查用户时就把订单一起拉出来
- Python 的 @property：访问时才计算

核心思想就一句话：用到的时候才创建/加载，而不是提前全部准备好。

ROI()

- Return On Investment（投资回报率），用来评估"做这件事值不值"。

灰度

- 即新版本项目先进行对一小部分人的发布或更新，而不对所有用户进行更新。
    - 这样如果有bug的话可以减少损失
    - 测试项目的并发压力范围

Code review

- 即项目开发或审核人员对想要并入自己项目的代码进行格式，规范和合理性进行检验

动态检测

- 比如单元测试，内存泄漏检测，性能分析等需要对代码运行起来的结果进行检测的一系列检测

CI/CD

- 应该是开发完之后对项目的编译部署什么的操作

渐进式披露

- AIagent中对Skills的具体操作，一个Skills可以分为两部分Skill.md规范，对应脚本，Aiagent在加载的时候只会加载其Skill.md中的简单介绍部分，剩下的如何使用和脚本都不会加载，而是等真正要使用的时候才会加载到对应上下文中

循环依赖(鸡生蛋、蛋生鸡)

- 用 Go 的话说：如果你 package A import 了 package B，同时
package B 又 import 了 package A，编译器直接报错，一个都跑不起来。

ReAct

- 思考后再行动，而不是直接行动